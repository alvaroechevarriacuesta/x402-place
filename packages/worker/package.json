{
  "name": "@x402place/worker",
  "version": "0.1.0",
  "private": true,
  "main": "./src/worker.ts",
  "scripts": {
    "dev": "tsx watch src/worker.ts",
    "start": "tsx src/worker.ts",
    "build": "esbuild src/worker.ts --bundle --platform=node --outfile=dist/worker.js --external:@prisma/client --external:.prisma/client"
  },
  "dependencies": {
    "@vercel/blob": "^2.0.0",
    "@x402place/shared": "workspace:*",
    "pngjs": "^7.0.0"
  },
  "devDependencies": {
    "@types/node": "^20",
    "@types/pngjs": "^6.0.5",
    "esbuild": "^0.24.0",
    "tsx": "^4.7.0",
    "typescript": "^5.6.3"
  }
}
